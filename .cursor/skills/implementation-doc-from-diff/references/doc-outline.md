# 基于改动的实现说明文档 — 章节骨架（可选）

按需删减；代码块内注释宜细，正文宜交代「为什么」。

**文件名（与 SKILL 一致）**：新建 `*.md` 时须**简短**、**贴本轮改动主题**（推荐 kebab-case）；避免 `notes.md` / `update.md` 等泛名。正文 `# 标题` 可与文件名不完全相同。

```markdown
# <特性或需求简述>

## 1. 背景与目标

## 2. 改动范围

- 涉及路径列表（仓库相对路径）

## 3. 实现思路

- 核心决策与权衡
- 数据流 / 调用链（可配简易列表或 mermaid）

## 4. 关键实现（带注释代码）

### 4.1 <模块 A>

说明；**每个代码块上方**写 **来源**：仓库相对路径 + 大致位置（`约 L起–L止` 或 函数名/区域描述）。

```lang
（讲解版注释）
```

### 4.2 <模块 B>

## 5. 行为变化与兼容性

- 对用户可见的变化
- 未改动路径的行为保证

## 6. 测试与回归建议

## 7. 相关文档与代码索引

| 说明 | 路径 |
```

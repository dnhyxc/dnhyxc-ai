# docs/ideas 实现思路 — 正文模板

落盘时按此骨架填写；**§4～§6 三图必填**。编号可微调，但不可删除「图」相关章节。

**文件名**：`docs/ideas/<简体中文短名>.md`（建议 4～12 字，语义明确；禁止英文 kebab-case 与泛名）；与 SKILL 硬约束 §1 一致。

---

```markdown
# <功能标题> — 实现思路

> **状态**：规划（未实现 / 部分已有能力可复用）  
> **日期**：YYYY-MM-DD  
> **需求摘要**：<谁 + 场景 + 做什么 + 成功标准，一句>

## 延伸阅读

- （可选）相关已有实现专题或 developer 手册链接
- （可选）相关 Influence-point 影响面（若已存在）

---

## 0. 读本文你将得到什么

用 3～5 条 bullet，让读者 30 秒内知道：

- 要解决什么问题
- 总体方案一句话
- 主要改哪些层（UI / API / Store …）
- 分几阶段落地
- 最大风险是什么

---

## 1. 需求与边界

### 1.1 用户故事

| 角色 | 场景 | 行为 | 期望结果 |
|------|------|------|----------|
| … | … | … | … |

### 1.2 范围

| 在范围内 | 不在范围内（非目标） |
|----------|----------------------|
| … | … |

### 1.3 约束与依赖

- 登录 / 会员 / 平台（Web、Tauri）
- 须复用的现有能力（点名模块）
- 性能或体验底线

---

## 2. 方案总览（一句话 + 要点）

**一句话方案**：<例如：「在 EpubPane resize 链路末尾 schedule 听书背景重绘，并令 repaintActive 重新挂载 SVG group」>

| # | 设计要点 | 理由 |
|---|----------|------|
| 1 | … | … |
| 2 | … | … |

---

## 3. 现状与复用

| 能力 | 仓库中已有 | 本需求中的用法 |
|------|------------|----------------|
| … | `path/to/module` | 扩展 / 直接复用 / 不适用 |

**调研结论**：<2～4 句：哪些不用重写、缺什么>

---

## 4. 架构图

（Mermaid `flowchart TB` 或 `graph LR` — 见 diagram-guide.md）

**图内方法说明**：

| 方法 / 模块入口 | 功能 |
|-----------------|------|
| `…()` | …（做什么、谁调用、关键副作用） |
| … | … |

**读图要点**：

- 分层关系：…
- 新增模块（标注 🆕）：…
- 与现有模块边界：…

---

## 5. 主流程图

（Mermaid `flowchart TD` — 含成功与主要失败分支）

**图内方法说明**：

| 方法 | 功能 |
|------|------|
| `…()` | … |

**读图要点**：

- 入口：…
- 关键分支：…
- 终止条件：…

---

## 6. 核心时序图

（Mermaid `sequenceDiagram` — Happy path，≥3 参与者）

**图内方法说明**：

| 方法 | 功能 |
|------|------|
| `…()` | … |

**读图要点**：

- 谁发起、谁响应：…
- 异步/轮询/SSE 若有须标出：…

---

## 7. （可选）状态机

仅当功能有明显模式切换（播放/暂停、编辑/预览）时填写。

（Mermaid `stateDiagram-v2`）

**图内方法说明**（若有 guard/触发函数则必填）：

| 方法 | 功能 |
|------|------|
| `…()` | … |

---

## 8. 模块职责与接口草图

### 8.1 模块一览

| 模块 | 职责 | 新增/改动 | 预估路径 |
|------|------|-----------|----------|
| … | … | 新增 | `apps/...` |

### 8.2 关键接口（伪代码或 TypeScript 草图）

```typescript
// 仅签名级草图，≤30 行/块
type Example = { ... };
function doSomething(input: Example): Result;
```

### 8.3 数据模型

| 字段/实体 | 来源 | 存储 | 说明 |
|-----------|------|------|------|
| … | … | DB / local / 内存 | … |

---

## 9. 分阶段实现步骤

| 阶段 | 目标 | 交付物 | 依赖 |
|------|------|--------|------|
| M1 | 最小可用 … | … | … |
| M2 | … | … | M1 |
| M3 |  polish / 边界 | … | M2 |

每阶段 2～5 条 **可勾选** 任务（`- [ ]`）。

---

## 10. 关键决策与备选方案

| 决策 | 选用 | 备选 | 为何不选备选 |
|------|------|------|--------------|
| … | A | B | … |

---

## 11. 风险、边界与待确认

| 项 | 等级 | 说明 | 缓解 |
|----|------|------|------|
| … | 高/中/低 | … | … |

**待确认**（若有）：

- [ ] …（验证方式：…）

---

## 12. 验收清单

| # | 用例 | 步骤 | 期望 |
|---|------|------|------|
| AC1 | … | … | … |

---

## 13. 预估改动面（实现阶段参考）

| 类型 | 路径（预估） |
|------|--------------|
| 前端 | … |
| 后端 | … |
| 文档（实现后） | `docs/<功能域>/...` |

---

（本文档为规划态实现思路；落地后以源码与 `docs/<功能域>/` 专题为准）
```

# dnhyxc-ai 代码改动 Wiki

本目录收录当前项目分支（`feature`）相对 `origin/feature` 的全部代码改动说明，按功能模块拆分。所有文档仅描述改动，不修改任何业务源码。

## 改动概览

本次改动共涉及 **78 个业务文件**（apps/ 目录，含 53 个修改 + 25 个新增），核心是为项目引入 **Skill（Prompt 指令包）系统**，并把 Agent 的消息存储按业务场景分表，同时让知识库助手接入具备工具调用 + Skill 强制加载能力的 Agent，以及把保存/新建快捷键提升为应用级统一监听。

### 功能模块清单

| # | 文档 | 核心内容 |
|---|---|---|
| 1 | [Agent 业务消息分表方案](./Agent业务消息分表方案.md) | `AgentTurnMemory` 端口 + 四套业务表（assistant / english / skill_try / agent）+ AgentService 路由 + 会话标题更新接口 |
| 2 | [Skill 系统后端](./Skill系统后端.md) | `skill` 表 / CRUD / 试跑会话 / `findByIdsForUser` 加载 / 草稿绑定 Skill |
| 3 | [Agent Skill 强制加载与 SSE](./AgentSkill强制加载与SSE.md) | 三重注入（系统提示 + 用户前缀 + 预置工具消息）+ `skillsApplied` SSE 帧 + error 兜底 |
| 4 | [知识库助手接入 Agent](./知识库助手接入Agent.md) | `sendMessageWithAgentSkills` + `agentSse` util + `/` 选择器 + intentPrefix 全文注入 |
| 5 | [Skill 前端](./Skill前端.md) | `skillStore` / `skillTryStore` / `SkillSlashPicker` / `/skills` 页面 / 路由导航 |
| 6 | [Skill 侧栏朗读与会话切换](./Skill侧栏朗读与会话切换.md) | 朗读条抬到页面防 remount 误断；`mode:skillId` 分桶；切 Skill/模式/历史显式停播；`panel` 气泡 |
| 7 | [Share 分表路由与 Assistant 改造](./Share分表路由与Assistant改造.md) | Share 按业务表路由取消息 + `applied_skills` 列 + import-transcript 支持 + 摘要表 |
| 8 | [数据库迁移](./数据库迁移.md) | 8 个迁移（4 空 4 实）：`skill_try_*` / `english_agent_*` / `assistant_session_summaries` / `applied_skills`（assistant + skill_try） |
| 9 | [文档级快捷键](./文档级快捷键.md) | `useDocumentShortcuts` 应用级统一监听保存/新建 + `bindDocumentShortcutHandlers` 页面注册 + 录制体验优化 |

### 阅读建议

1. 先读 **Agent 业务消息分表方案**，理解整体架构（`AgentTurnMemory` 端口 + 四套表路由）。
2. 再读 **Skill 系统后端** 和 **Agent Skill 强制加载与 SSE**，理解 Skill 从存储到注入模型的全链路。
3. 然后读 **知识库助手接入 Agent** 和 **Skill 前端**，理解前端如何使用 Skill。
4. 若手测侧栏：再读 **Skill 侧栏朗读与会话切换**（朗读生命周期 + `mode:skillId` 分桶，修正「开合库断播 / 试跑生成串台」）。
5. 再读 **文档级快捷键**，理解保存/新建快捷键如何从页面内提升为应用级。
6. 最后读 **Share 分表路由与 Assistant 改造** 和 **数据库迁移**，了解配套改造。

### 关键技术决策

- **消息分表而非分区**：英语学习 / Skill 试跑 / 知识库助手各有独立表，历史列表互不污染。
- **`AgentTurnMemory` 端口**：AgentService 不关心消息落哪张表，运行时按 `memorySource` 选实现。
- **Skill 三重注入**：系统提示硬约束 + 用户前缀钉死 + 预置 `apply_skill` 工具消息，确保模型必执行 Skill。
- **SSE error 用 next 而非 error**：Nest SSE 中途 `subscriber.error` 常丢帧，统一用 `next({ type: 'error' })` + `complete`。
- **appliedSkills 双写**：流开始立即写快照（防崩溃丢失），流结束再带正文更新同一行；`assistant_messages` 和 `skill_try_messages` 均有此列。
- **文档级快捷键**：Layout 全局监听 keydown（捕获阶段），页面通过 `bindDocumentShortcutHandlers` 注册回调，模块级 `handlers` 变量实现跨组件通信。
- **Skill 侧栏 scope**：`` `${mode}:${skillId|__draft__}` `` 分桶；朗读 hook 挂在 `SkillsPage`，避免 `ResizablePanelGroup` remount 断播。

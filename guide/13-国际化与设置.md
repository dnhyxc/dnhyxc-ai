# 十三、国际化与设置

## 13.1 国际化（i18n）

### 13.1.1 useSyncExternalStore 驱动

```typescript
// hooks/i18n.ts
import { useSyncExternalStore } from 'react';

// 模块级状态
let currentLocale: Locale = getInitialLocale();
const localeListeners = new Set<() => void>();

function emitLocaleChanged() {
  localeListeners.forEach(fn => fn());
}

function subscribe(listener: () => void) {
  localeListeners.add(listener);
  return () => {
    localeListeners.delete(listener);
  };
}

function getSnapshot() {
  return currentLocale;
}

export function useI18n() {
  const locale = useSyncExternalStore(subscribe, getSnapshot, getSnapshot);

  const setLocale = (newLocale: Locale, opts?: { syncUrl?: boolean }) => {
    currentLocale = newLocale;

    // 持久化
    localStorage.setItem('dnhyxc_locale_bootstrap', newLocale);

    // 同步 URL
    if (opts?.syncUrl) {
      const url = new URL(window.location.href);
      url.searchParams.set('lang', newLocale);
      window.history.replaceState({}, '', url.toString());
    }

    emitLocaleChanged();
  };

  const t = useCallback((key: string, params?: Record<string, string>) => {
    const dict = DICTS[locale] || DICTS[DEFAULT_LOCALE];
    let template = dict[key] || DICTS[DEFAULT_LOCALE][key] || key;

    // 参数插值
    if (params) {
      template = template.replace(
        /\{(\w+)\}/g,
        (_, k) => params[k] ?? `{${k}}`,
      );
    }

    return template;
  }, [locale]);

  return {
    locale,
    t,
    setLocale,
    toggleLocale: () => {
      setLocale(locale === 'zh-CN' ? 'en-US' : 'zh-CN', { syncUrl: true });
    },
    supportedLocales: SUPPORTED_LOCALES,
  };
}
```

### 13.1.2 初始化优先级

```typescript
function getInitialLocale(): Locale {
  // 1. URL 参数
  const urlParams = new URLSearchParams(window.location.search);
  const langFromUrl = urlParams.get('lang') as Locale | null;
  if (langFromUrl && SUPPORTED_LOCALES.includes(langFromUrl)) {
    return langFromUrl;
  }

  // 2. localStorage bootstrap
  const bootstrap = localStorage.getItem('dnhyxc_locale_bootstrap') as Locale | null;
  if (bootstrap && SUPPORTED_LOCALES.includes(bootstrap)) {
    return bootstrap;
  }

  // 3. Tauri 异步持久化（通过 invoke 获取）
  // 注意：不能在模块加载时调用 invoke，需在 useEffect 中
  // 此处仅作降级处理

  // 4. 默认
  return DEFAULT_LOCALE;
}
```

### 13.1.3 词典结构

```typescript
// i18n/index.ts
export type Locale = 'zh-CN' | 'en-US';

export const SUPPORTED_LOCALES: Locale[] = ['zh-CN', 'en-US'];
export const DEFAULT_LOCALE: Locale = 'zh-CN';

export const DICTS: Record<Locale, Record<string, string>> = {
  'zh-CN': {
    'route.chat': '对话',
    'route.knowledge': '知识库',
    'route.englishLearning': '英语学习',
    'route.ebook': '电子书',
    'route.video': '视频',
    'setting.title': '设置',
    'setting.theme': '主题',
    'setting.llm': '大模型',
    'setting.cloudTts': '语音设置',
    'setting.system': '系统',
    'common.confirm': '确认',
    'common.cancel': '取消',
    'common.save': '保存',
    'common.loading': '加载中...',
    'common.networkErrorTryAgain': '网络请求失败，请重试',
    'chat.send': '发送',
    'chat.stop': '停止',
    'chat.continue': '续写',
    'chat.newChat': '新对话',
    'chat.search': '联网搜索',
    'chat.attachFile': '附件',
  },
  'en-US': {
    'route.chat': 'Chat',
    'route.knowledge': 'Knowledge',
    'route.englishLearning': 'English',
    'route.ebook': 'Ebook',
    'route.video': 'Video',
    'setting.title': 'Settings',
    'setting.theme': 'Theme',
    'setting.llm': 'AI Model',
    'setting.cloudTts': 'Voice',
    'setting.system': 'System',
    'common.confirm': 'Confirm',
    'common.cancel': 'Cancel',
    'common.save': 'Save',
    'common.loading': 'Loading...',
    'common.networkErrorTryAgain': 'Network error, please try again',
    'chat.send': 'Send',
    'chat.stop': 'Stop',
    'chat.continue': 'Continue',
    'chat.newChat': 'New Chat',
    'chat.search': 'Web Search',
    'chat.attachFile': 'Attach',
  },
};
```

### 13.1.4 组件透传

```typescript
// 可选 t prop 注入
interface MyComponentProps {
  t?: (key: string, params?: Record<string, string>) => string;
}

function MyComponent({ t: tProp }: MyComponentProps) {
  const { t: tHook } = useI18n();
  const t = tProp || tHook;  // 优先使用注入的 t

  return <div>{t('common.loading')}</div>;
}
```

---

## 13.2 主题色（强调色）

### 13.2.1 10 色预设

```typescript
// hooks/theme.ts
export const ACCENT_COLORS: AccentColor[] = [
  { id: 'default', hex: '#14b8a6', labelKey: 'accent.default', descKey: 'accent.default.desc' },
  { id: 'lime', hex: '#84cc16', labelKey: 'accent.lime', descKey: 'accent.lime.desc' },
  { id: 'rose', hex: '#f43f5e', labelKey: 'accent.rose', descKey: 'accent.rose.desc' },
  { id: 'indigo', hex: '#6366f1', labelKey: 'accent.indigo', descKey: 'accent.indigo.desc' },
  { id: 'ochre', hex: '#b45309', labelKey: 'accent.ochre', descKey: 'accent.ochre.desc' },
  { id: 'lutea', hex: '#ca8a04', labelKey: 'accent.lutea', descKey: 'accent.lutea.desc' },
  { id: 'apricot', hex: '#ea580c', labelKey: 'accent.apricot', descKey: 'accent.apricot.desc' },
  { id: 'daiqing', hex: '#3b82f6', labelKey: 'accent.daiqing', descKey: 'accent.daiqing.desc' },
  { id: 'songhua', hex: '#0ea5e9', labelKey: 'accent.songhua', descKey: 'accent.songhua.desc' },
  { id: 'cangcui', hex: '#10b981', labelKey: 'accent.cangcui', descKey: 'accent.cangcui.desc' },
];
```

### 13.2.2 现场混色

```typescript
function applyAccentToDocument(colorId: string) {
  const color = ACCENT_COLORS.find(c => c.id === colorId);
  if (!color) return;

  const root = document.documentElement;

  // 主色
  root.style.setProperty('--brand-accent', color.hex);

  // 衍生色（使用 color-mix 现场计算）
  root.style.setProperty('--brand-accent-soft',
    colorMix('oklch', color.hex, 'white', 0.85));
  root.style.setProperty('--brand-accent-light',
    colorMix('oklch', color.hex, 'white', 0.6));
  root.style.setProperty('--brand-accent-dark',
    colorMix('oklch', color.hex, 'black', 0.2));

  // 覆盖 Tailwind teal 类
  // text-teal-500 → text-[var(--brand-accent)]
  // bg-teal-500 → bg-[var(--brand-accent)]
  // bg-teal-500/15 → bg-[var(--brand-accent)]/15
}
```

### 13.2.3 首帧防闪

```html
<!-- index.html 内联脚本 -->
<script>
  (function() {
    try {
      const accent = localStorage.getItem('dnhyxc_accent_bootstrap');
      if (accent) {
        const color = JSON.parse(accent);
        const root = document.documentElement;
        root.style.setProperty('--brand-accent', color.hex);
        // 立即应用，React 挂载后再校正
      }
    } catch(e) {}
  })();
</script>
```

### 13.2.4 持久化

```typescript
function persistAccentBootstrap(colorId: string) {
  const color = ACCENT_COLORS.find(c => c.id === colorId);
  if (!color) return;

  // localStorage（首帧读取）
  localStorage.setItem('dnhyxc_accent_bootstrap', JSON.stringify({
    id: color.id,
    hex: color.hex,
  }));

  // Tauri store（跨端同步）
  if (isTauriRuntime()) {
    invoke('set_store', {
      key: 'accentColor',
      value: colorId,
    });
  }
}
```

---

## 13.3 系统快捷键

### 13.3.1 快捷键定义与分类

```typescript
// views/setting/system/config.ts
export const DEFAULT_INFO: ShortcutSettingItem[] = [
  // 全局快捷键（Tauri 注册，registerGlobally: true）
  { key: '1', shortcut: 'Meta+H', registerGlobally: true, labelKey: 'shortcut.toggleApp' },
  { key: '2', shortcut: 'Meta+N', registerGlobally: true, labelKey: 'shortcut.newChat' },
  { key: '3', shortcut: 'Meta+K', registerGlobally: true, labelKey: 'shortcut.newKnowledge' },
  { key: '4', shortcut: 'Meta+W', registerGlobally: true, labelKey: 'shortcut.switchWindow' },
  { key: '5', shortcut: 'Meta+,', registerGlobally: true, labelKey: 'shortcut.settings' },

  // 通用：保存 / 新建（应用级，由 Layout 统一监听）
  { key: '6', shortcut: 'Meta+S', registerGlobally: false, label: '通用：保存', action: 'knowledge_save' },
  { key: '7', shortcut: 'Meta+Shift+N', registerGlobally: false, label: '通用：新建', action: 'knowledge_clear' },

  // 知识库：导入 / 打开库 / 回收站 / 发送选区
  { key: '8', shortcut: 'Meta+I', registerGlobally: false, label: '知识库：导入文件', action: 'knowledge_import' },
  // ...

  // 编辑器：操作栏切换 / 编辑源码 / 分屏对照 / 预览渲染
  { key: '9', shortcut: '...', registerGlobally: false, label: '编辑器：切换操作栏', action: 'knowledge_toggle_markdown_bottom_bar' },
  // ...
];
```

**分类说明**：

| 分类 | 含义 | 监听方式 |
|------|------|----------|
| 全局 | Tauri 注册系统级快捷键 | `registerGlobally: true` |
| 通用 | 保存 / 新建，跨页面复用 | `Layout` 统一 `keydown` 监听 |
| 知识库 | 知识库专属操作 | 页面内监听 |
| 编辑器 | Monaco 底部操作栏 | 编辑器内监听 |

### 13.3.2 应用级文档快捷键（保存 / 新建）

「保存」「新建」从知识库页面级提升为**应用级**，由 `Layout` 统一监听：

```typescript
// hooks/useDocumentShortcuts.ts
// 模块级回调注册表
let handlers: { onSave?: () => void; onNew?: () => void } = {};

// 页面注册回调，返回解绑函数
export function bindDocumentShortcutHandlers(next) {
  handlers = { ...handlers, ...next };
  return () => {
    if (next.onSave && handlers.onSave === next.onSave) handlers.onSave = undefined;
    if (next.onNew && handlers.onNew === next.onNew) handlers.onNew = undefined;
  };
}

// Layout 挂载：统一监听 window keydown（capture 阶段）
export function useDocumentShortcuts() {
  useEffect(() => {
    const onKeyDown = (e) => {
      if (chordMatchesStored(saveChord, e)) { handlers.onSave?.(); e.preventDefault(); }
      if (chordMatchesStored(newChord, e)) { handlers.onNew?.(); e.preventDefault(); }
    };
    window.addEventListener('keydown', onKeyDown, true);
    return () => window.removeEventListener('keydown', onKeyDown, true);
  }, []);
}
```

- 页面 `useEffect` 中调用 `bindDocumentShortcutHandlers({ onSave, onNew })` 注册；卸载自动解绑。
- 解绑时检查**同一函数引用**，防止误清后续页面注册的回调。
- chord 从 `knowledge-shortcuts` 加载，监听变更事件实时刷新。

### 13.3.3 Rust 侧解析

```rust
// src-tauri/src/system/shortcut.rs
pub fn parse_chord(chord: &str) -> Result<(Vec<KeyCode>, Modifiers), String> {
    let parts: Vec<&str> = chord.split('+').collect();
    let mut keys = Vec::new();
    let mut modifiers = Modifiers::empty();

    for part in parts {
        match part.to_lowercase().as_str() {
            "meta" | "cmd" | "command" => modifiers |= Modifiers::SUPER,
            "ctrl" | "control" => modifiers |= Modifiers::CONTROL,
            "shift" => modifiers |= Modifiers::SHIFT,
            "alt" | "option" => modifiers |= Modifiers::ALT,
            _ => {
                if let Ok(key) = parse_key_code(part) {
                    keys.push(key);
                }
            }
        }
    }

    Ok((keys, modifiers))
}
```

### 13.3.4 冲突检测

```typescript
// views/setting/system/index.tsx
function chordStringsSemanticallyEqual(a: string, b: string): boolean {
  const normalize = (s: string) => s
    .toLowerCase()
    .split('+')
    .map(k => k.trim())
    .sort()
    .join('+');

  return normalize(a) === normalize(b);
}

function detectConflicts(newShortcut: string, currentKey: string): string | null {
  for (const item of DEFAULT_INFO) {
    if (item.key !== currentKey && chordStringsSemanticallyEqual(item.shortcut, newShortcut)) {
      return item.labelKey;
    }
  }
  return null;
}
```

专题：[docs/app/文档快捷键统一.md](../docs/app/文档快捷键统一.md)

---

## 13.4 设置页架构

### 13.4.1 双维度设置

```
┌─────────────────────────────────────────┐
│              设置页 Layout                │
│  ┌──────────┐  ┌──────────────────────┐ │
│  │ 侧边导航  │  │     主内容区           │ │
│  │          │  │                      │ │
│  │ · 主题色  │  │  ┌────────────────┐  │ │
│  │ · 大模型  │  │  │ 强调色选择器    │  │ │
│  │ · 语音   │  │  │ (10 色预设)     │  │ │
│  │ · 系统   │  │  └────────────────┘  │ │
│  │ · 账户   │  │  ┌────────────────┐  │ │
│  │          │  │  │ 彩色主题       │  │ │
│  │          │  │  │ (light/dark/..) │  │ │
│  │          │  │  └────────────────┘  │ │
│  │          │  │  ┌────────────────┐  │ │
│  │          │  │  │ 快捷键录制     │  │ │
│  │          │  │  └────────────────┘  │ │
│  └──────────┘  └──────────────────────┘ │
└─────────────────────────────────────────┘
```

### 13.4.2 设置路由

```typescript
// router/routes.ts
const settingsRoutes = [
  { path: '/setting/theme', component: ThemeSettingPage, meta: { titleKey: 'setting.theme' } },
  { path: '/setting/llm', component: LlmSettingPage, meta: { titleKey: 'setting.llm' } },
  { path: '/setting/cloud-tts', component: TtsSettingPage, meta: { titleKey: 'setting.cloudTts' } },
  { path: '/setting/system', component: SystemSettingPage, meta: { titleKey: 'setting.system' } },
  { path: '/setting/profile', component: ProfilePage, meta: { titleKey: 'setting.profile' } },
];
```

---

## 13.5 配置要点

| 配置 | 说明 |
|------|------|
| `DEFAULT_LOCALE` | 默认 `zh-CN` |
| `LOCALE_BOOTSTRAP_STORAGE_KEY` | `dnhyxc_locale_bootstrap` |
| `ACCENT_COLORS` | 10 色预设 |
| Tailwind `--color-teal-*` | 映射到 `--brand-accent*` |
| `ShortcutSettingItem.registerGlobally` | true 为 Tauri 全局 |
| localStorage + Tauri store | 双写持久化 |

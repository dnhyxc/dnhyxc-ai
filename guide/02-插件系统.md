# 二、插件系统

## 2.1 系统架构

### 2.1.1 分层模型

本系统采用 **Module Federation (MF)** 为核心的动态微前端架构，通过分层设计实现核心逻辑与宿主逻辑的解耦：

```
┌─────────────────────────────────────────┐
│           宿主应用 (Host)                 │
│  ┌───────────────────────────────────┐  │
│  │  PluginHostPage (容器 + 样式隔离)  │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  PluginManager (加载/校验/生命周期) │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  HostBridge (通信/导航/数据)      │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  Module Federation Runtime        │  │
│  └───────────────────────────────────┘  │
├─────────────────────────────────────────┤
│           插件 (Remote)                   │
│  ┌───────────────────────────────────┐  │
│  │  PluginDescriptor (元数据)         │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  expose: ./App (组件/逻辑)        │  │
│  └───────────────────────────────────┘  │
│  ┌───────────────────────────────────┐  │
│  │  activate/deactivate (生命周期)   │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

### 2.1.2 核心模块

| 模块 | 路径 | 职责 |
|------|------|------|
| **PluginManager** | `src/plugins/core/PluginManager.ts` | 插件加载、校验、生命周期管理 |
| **PluginVerifier** | `src/plugins/core/PluginVerifier.ts` | 插件合法性验证 |
| **HostBridge** | `src/plugins/bridge/HostBridge.ts` | 宿主与插件间通信 |
| **PluginHostPage** | `src/plugins/host/PluginHostPage.tsx` | 插件容器、样式隔离 |
| **styleIsolation** | `src/plugins/host/styleIsolation.ts` | CSS 样式隔离核心 |
| **federation-kit** | `packages/federation-kit/` | 共享逻辑包 |

### 2.1.3 信任等级

| 等级 | 技术方案 | 隔离程度 | 适用场景 |
|------|----------|---------|---------|
| **trusted** | MF + CSS @scope | 样式级 | 可信内部插件 |
| **untrusted** | iframe 完全隔离 | 完全 | 不可信第三方插件 |

---

## 2.2 Module Federation 实现

### 2.2.1 宿主配置

```typescript
// apps/frontend/vite.config.ts
federation({
  name: 'host',
  filename: 'remoteEntry.js',
  remotes: {
    // 运行时动态注册，不在构建时写死
  },
  shared: {
    react: { singleton: true, requiredVersion: '^19.1.0' },
    'react-dom': { singleton: true, requiredVersion: '^19.1.0' },
    'react-router-dom': { singleton: true },
    zustand: { singleton: true },
  },
  dev: {
    // 开发态 HMR
    remoteHmr: true,
  },
});
```

### 2.2.2 插件配置

```typescript
// plugins/my-plugin/vite.config.ts
federation({
  name: 'my-plugin',
  filename: 'remoteEntry.js',
  exposes: {
    './App': './src/App.tsx',
    './Panel': './src/Panel.tsx',
  },
  shared: {
    react: { singleton: true, requiredVersion: '^19.1.0' },
    'react-dom': { singleton: true, requiredVersion: '^19.1.0' },
  },
  dev: {
    remoteHmr: true,
  },
});
```

### 2.2.3 动态加载流程

```
1. PluginManager.ensurePlugin(pluginId)
   │
   ├─ verifyPlugin(meta)          // 校验元数据
   ├─ registerRemote(meta)       // MF Runtime 注册
   │   └─ __federation_method_setRemote({ entry, name })
   │
   ├─ beginPluginStyleCapture()   // 开启样式捕获
   │
   ├─ loadRemoteApp(meta)         // MF 动态加载
   │   └─ __federation_method_remotes[url].get(module)
   │       → Promise<{ default: Component, activate?, deactivate? }>
   │
   ├─ endCapture()                // 结束样式捕获
   ├─ mod.activate?.(bridge.api)  // 调用激活钩子
   │
   └─ 状态 → 'activated'
```

### 2.2.4 PluginRegistry 数据结构

```typescript
interface PluginDescriptor {
  id: string;                // 唯一标识
  name: string;              // 显示名称
  version: string;           // 版本号
  entry: string;             // remoteEntry.js URL
  expose: string;            // 暴露模块路径，如 './App'
  icon?: string;             // 图标 URL/SVG
  description?: string;      // 描述
  trust: 'trusted' | 'untrusted';  // 信任等级
  iframeUrl?: string;        // untrusted 模式 iframe URL
  menu?: {                   // 菜单配置
    label: string;
    order?: number;
  };
}
```

---

## 2.3 插件开发完整流程

### 2.3.1 创建插件项目

```bash
# 使用模板创建
pnpm create mf-plugin my-plugin

# 或手动创建
mkdir -p my-plugin/src
cd my-plugin
pnpm init
pnpm add react react-dom
pnpm add -D vite @originjs/vite-plugin-federation typescript
```

### 2.3.2 编写插件入口

```typescript
// src/App.tsx
import React from 'react';

export default function App() {
  return (
    <div className="my-plugin">
      <h1>My Plugin</h1>
      <p>Hello from the plugin!</p>
    </div>
  );
}

// 可选：生命周期钩子
export async function activate(api: HostBridgeAPI) {
  console.log('Plugin activated:', api);
  // api.navigate('/some-route');
  // api.getData();
}

export async function deactivate() {
  console.log('Plugin deactivated');
}
```

### 2.3.3 配置插件

```typescript
// vite.config.ts
import { defineConfig } from 'vite';
import federation from '@originjs/vite-plugin-federation';

export default defineConfig({
  plugins: [
    federation({
      name: 'my-plugin',
      filename: 'remoteEntry.js',
      exposes: { './App': './src/App.tsx' },
      shared: { react: { singleton: true }, 'react-dom': { singleton: true } },
    }),
  ],
  build: { target: 'esnext', modulePreload: false, cssCodeSplit: false },
});
```

### 2.3.4 构建与发布

```bash
# 构建
pnpm run build

# 产物
# dist/
# ├── remoteEntry.js
# ├── assets/
# │   ├── index-xx.js
# │   └── remoteEntryEntry.js.map

# 上传到 CDN
aws s3 sync dist/ s3://my-bucket/plugins/my-plugin/
```

### 2.3.5 注册到宿主

在宿主的 Plugin Registry 中注册插件元数据：

```typescript
{
  id: 'my-plugin',
  name: 'My Plugin',
  version: '1.0.0',
  entry: 'https://cdn.example.com/plugins/my-plugin/remoteEntry.js',
  expose: './App',
  trust: 'trusted',
  menu: { label: '我的插件', order: 10 },
}
```

---

## 2.4 宿主接入方式

### 2.4.1 自动注入（路由 + 菜单）

```typescript
// plugins/core/PluginManager.ts
private async runLoad(meta: PluginDescriptor) {
  // 加载插件
  const mod = await loadRemoteApp(meta);
  const bridge = createHostBridge(meta, nav);

  // 自动注册路由
  if (meta.expose === './App') {
    RouteInjector.register({
      path: `/plugins/${meta.id}`,
      element: <PluginHostPage pluginId={meta.id} />,
    });
  }

  // 自动注入菜单
  if (meta.menu) {
    SidebarInjector.addItem({
      id: meta.id,
      label: meta.menu.label,
      to: `/plugins/${meta.id}`,
      icon: meta.icon,
      order: meta.menu.order,
    });
  }

  // 调用激活钩子
  await mod.activate?.(bridge.api);
}
```

### 2.4.2 手动挂载（嵌入式插件）

```typescript
// 在业务组件中手动挂载
import { usePluginHost } from '@/plugins/host/usePluginHost';

function EmbeddedPluginPanel({ pluginId }: { pluginId: string }) {
  const { loaded, bridge, status } = usePluginHost(pluginId);

  if (status === 'loading') return <div>Loading...</div>;
  if (status === 'error') return <div>Error: {loaded?.error}</div>;

  const Comp = loaded?.mod.default;
  if (!Comp) return null;

  return (
    <div data-mf-plugin={pluginId}>
      <Comp {...bridge} />
    </div>
  );
}
```

### 2.4.3 iframe 隔离（untrusted 模式）

```typescript
// PluginHostPage.tsx
if (loaded.meta.trust === 'untrusted') {
  return (
    <PluginErrorBoundary pluginId={pluginId}>
      <UntrustedIframe
        pluginId={pluginId}
        src={loaded.meta.iframeUrl}
        bridge={loaded.bridge}
      />
    </PluginErrorBoundary>
  );
}

// UntrustedIframe.tsx
function UntrustedIframe({ src, bridge }) {
  const iframeRef = useRef<HTMLIFrameElement>(null);

  useEffect(() => {
    const handler = (e: MessageEvent) => {
      if (e.data?.type === 'plugin-init') {
        iframeRef.current?.contentWindow?.postMessage({
          type: 'host-ready',
          api: bridge.api,
        }, '*');
      }
    };
    window.addEventListener('message', handler);
    return () => window.removeEventListener('message', handler);
  }, [bridge]);

  return (
    <iframe
      ref={iframeRef}
      src={src}
      className="h-full w-full border-0"
      sandbox="allow-scripts allow-same-origin"
    />
  );
}
```

---

## 2.5 缓存破坏与 HMR

### 2.5.1 Manifest Hash 缓存破坏

```typescript
// plugins/host/pluginEntryCacheBust.ts
export async function resolvePluginBust(meta: PluginDescriptor) {
  const { buildId } = await fetchManifestMeta(meta.entry);
  return {
    entry: meta.entry,
    cacheBustedUrl: `${meta.version}@${buildId}/remoteEntry.js`,
  };
}

async function fetchManifestMeta(entryUrl: string) {
  const manifestUrl = entryUrl.replace('remoteEntry.js', 'manifest.json');
  const res = await fetch(manifestUrl);
  if (!res.ok) return { buildId: Date.now().toString() };
  return res.json();
}
```

### 2.5.2 HMR 热更新

```typescript
// 插件 vite.config.ts
federation({
  dev: { remoteHmr: true },
});

// 宿主 vite.config.ts
optimizeDeps: {
  include: [
    // 预构建频繁变化的依赖
    'react', 'react-dom', 'react-router-dom',
  ],
},

// PluginHostPage useEffect 中处理 HMR
useEffect(() => {
  // MF Runtime 会自动处理 HMR
  // 但需要确保组件使用默认导出
}, [loaded]);
```

---

## 2.6 Vue 插件桥接

### 2.6.1 插件端（Vue）

```typescript
// 插件入口必须导出 mount/unmount
export async function mount(container: HTMLElement, props: any) {
  const { createApp } = await import('vue');
  const App = await import('./App.vue');
  const app = createApp(App.default || App);
  app.mount(container);
  (container as any).__vue_app__ = app;
}

export async function unmount(container: HTMLElement) {
  const app = (container as any).__vue_app__;
  if (app) {
    app.unmount();
    delete (container as any).__vue_app__;
  }
}
```

### 2.6.2 宿主端桥接

```typescript
// plugins/host/createVueHostBridge.tsx
export function createVueHostBridge(
  expose: { mount: Function; unmount: Function },
  pluginId: string,
) {
  return function VueHostBridge(props: any) {
    const elRef = useRef<HTMLDivElement>(null);

    useEffect(() => {
      if (!elRef.current) return;
      expose.mount(elRef.current, props);
      return () => expose.unmount();
    }, []);

    return <div ref={elRef} data-plugin-root />;
  };
}
```

---

## 2.7 插件图标系统

### 2.7.1 存储与渲染

```typescript
// components/PluginIcon.tsx
interface PluginIconProps {
  src: string;         // SVG URL 或 markup
  size?: number;
  animated?: 'spin' | 'pulse';
}

export function PluginIcon({ src, size = 24, animated }: PluginIconProps) {
  const [svgHtml, setSvgHtml] = useState<string | null>(null);
  const [error, setError] = useState(false);

  useEffect(() => {
    if (src.startsWith('<svg')) {
      // 内联 SVG
      setSvgHtml(normalizeSvgForHostIcon(src));
    } else {
      // fetch SVG
      fetch(src)
        .then(res => res.text())
        .then(text => setSvgHtml(normalizeSvgForHostIcon(text)))
        .catch(() => setError(true));
    }
  }, [src]);

  if (error || !svgHtml) {
    return <DefaultPluginIcon size={size} />;
  }

  return (
    <span
      className={animated ? `animate-${animated}` : ''}
      style={{ width: size, height: size }}
      dangerouslySetInnerHTML={{ __html: svgHtml }}
    />
  );
}
```

### 2.7.2 SVG 规范化

```typescript
function normalizeSvgForHostIcon(svgText: string): string {
  // 处理 currentColor
  let html = svgText
    .replace(/currentColor/g, 'var(--brand-accent, currentColor)');

  // 处理 stroke/fill 属性
  html = html.replace(
    /<svg([^>]*)>/,
    '<svg$1 width="100%" height="100%" fill="currentColor" stroke="currentColor">'
  );

  return html;
}
```

---

## 2.8 插件偏好持久化

### 2.8.1 状态流转

```
用户开启插件
    │
    ├─ POST /api/plugins/enable  → 后端存储用户偏好
    │   └─ PluginUserPrefs 表：{ user_id, enabled_ids: JSON[] }
    │
    ├─ PluginManager.ensurePlugin(pluginId)
    │   └─ loadRemoteApp → mount → status: 'activated'
    │
    ├─ RouteInjector / SidebarInjector 注册入口
    │
    └─ 前端 localStorage 缓存（首屏同步）

用户关闭插件
    │
    ├─ DELETE /api/plugins/disable → 更新后端偏好
    ├─ PluginManager.unloadPlugin(pluginId)
    │   └─ mod.deactivate?.() → unmount → status: 'unloaded'
    └─ 路由/菜单入口移除
```

### 2.8.2 失败状态处理

```typescript
// PluginHostPage.tsx
if (loaded?.status === 'failed') {
  return (
    <div className="flex h-full flex-col items-center justify-center gap-4 p-8">
      <p className="text-muted-foreground">插件加载失败</p>
      <p className="text-sm text-muted-foreground">{loaded.error}</p>
      <div className="flex gap-2">
        <button onClick={() => setRetryKey(n => n + 1)}>重新加载</button>
        <button onClick={() => navigate('/')}>返回</button>
      </div>
    </div>
  );
}
```

---

## 2.9 插件开发 Checklist

### 创建新插件

- [ ] 创建插件项目目录
- [ ] 配置 `federation()` 暴露入口
- [ ] 实现默认导出组件
- [ ] 可选实现 `activate`/`deactivate` 生命周期
- [ ] 样式使用标准 Tailwind，无需手动 scoped
- [ ] 构建产物上传至 CDN
- [ ] 在 PluginRegistry 注册元数据
- [ ] 配置菜单和路由

### 验证检查

- [ ] 插件能正常加载和卸载
- [ ] 样式不影响宿主和其他插件
- [ ] HMR 在开发态正常工作
- [ ] untrusted 插件在 iframe 中正常运行
- [ ] 缓存破坏后能加载最新版本
- [ ] 主题同步正常（深色/浅色模式）
- [ ] 路由和菜单正确注入

---

## 2.10 插件 iframe 主题与强调色下发

> 完整实现说明见 [plugins/插件iframe主题下发.md](../docs/plugins/插件iframe主题下发.md)。

### 2.10.1 问题背景

`untrusted` 远程插件运行在跨域 iframe 内。改动前握手只下发 `theme: 'light' | 'dark'` 字符串，导致：

1. iframe 内强调色（`--brand-accent` 等）无法跟随主站「设置 → 强调色」实时变化。
2. 主站常用 `body.theme-black` 表示暗色而不挂 `.dark`，但 iframe 强行加 `.dark` 会误触 `dark:border-input`，导致 outline 按钮边框消失。
3. React Strict Mode 与路由切换时 `attachIframeBridge` 存在 `ready` 重复应答、内存泄漏、桌面 WebView 白屏。
4. `useInputsOnlyTab` 把 `iframe` 标 `tabIndex=-1`，夺走插件内部 Tab 序，与 Radix Dialog/Sheet 焦点陷阱互殴。

### 2.10.2 HostIframeAppearance 快照方案

宿主把当前外观打包成 `HostIframeAppearance` 快照，通过 `postMessage('appearance', ...)` 下发，iframe 侧自行 `setProperty` 应用，避免跨域 DOM 写入：

```typescript
export type HostIframeAppearance = {
  theme: HostTheme;           // light / dark
  cssVars: Record<string, string>; // --brand-accent / --background 等
  darkClass?: boolean;         // 是否给 iframe 加 .dark
};
```

### 2.10.3 关键策略

| 策略 | 说明 |
| ---- | ---- |
| 强调色与表面 token 分层 | 始终下发强调色四件套；只在主站无 `.dark` 但 theme 为 dark 时下发表面 token；绝不给无 `.dark` 主站强加 `.dark` |
| 指纹去重 + 80ms 防抖 | `theme\|darkClass\|JSON.stringify(cssVars)` 作指纹，未变即跳过；防抖避开路由卸载同帧 |
| 握手单次 + load 不占 ready 槽 | `readyAcked` 标志防止 Strict Mode 双应答；`onLoad` 只发 `init` 兜底 |
| 路由切换先 about:blank | 卸载 iframe 前先 `src = 'about:blank'`，避免桌面 WebView 主线程卡死 |
| ref 稳定 identity | `UntrustedIframe` 用 `useRef` 持最新值，effect 只依赖 `src`，避免 detach/attach 抖动 |
| Tab 序不夺 iframe 焦点 | `useInputsOnlyTab` selector 移除 `iframe`，扫描跳过 Radix Dialog/Sheet 内元素 |

### 2.10.4 能力接入

```typescript
// createFederation / PluginHostPage 透传
const host = useMemo(() => createFederation({
  capabilities: {
    getAppearance: readHostIframeAppearance,
    onAppearanceChange: (cb) => onAppearanceChange(cb),
  },
}), []);
```

能力可选——不提供时 `attachIframeBridge` 回退到仅下发 `theme` 字符串，既有接入方零改动。

---

## 2.11 插件中心 Tab 分类与卡片重构

> 完整实现说明见 [plugins/插件中心卡片重构.md](../docs/plugins/插件中心卡片重构.md)。

### 2.11.1 问题背景

改动前插件中心是扁平列表，所有插件不分类型堆在一起，卡片信息密度低且无图标、无「进入入口」、无法区分「业务内嵌插件」与「独立路由应用」。

### 2.11.2 Tab 分类推断

registry 无显式 `kind` 字段，按现有结构推断 `catalogKind`：

```typescript
function catalogKind(p: PluginDescriptor): CatalogTab {
  if (p.host || p.injectRoute === false) return 'plugin'; // 业务内嵌/surface 槽
  return 'app';                                            // 独立路由注入
}
```

- **plugin**：`host` 存在（业务面内嵌）或 `injectRoute === false`（宿主已挂 `PluginHostPage`）。
- **app**：`injectRoute` 非 false，由 `PluginManager` 注入顶层路由。

### 2.11.3 卡片三段式结构

卡片由「`CardHeader` + `CardContent`」重构为三段式：

| 区 | 内容 |
| ---- | ---- |
| `CardHeader` | 图标（`PluginIcon` / `AppWindow` / `Puzzle` 兜底）+ 标题 + `id · v版本` + 上架开关 |
| `CardDescription` | 描述（`line-clamp-2`） |
| `CardFooter` | 路由 · 侧栏有无 · 信任级别（单行 truncate）+ 应用类「进入」按钮 |

- `CardFooter` 用 `mt-auto` 贴底，保证一排卡片底栏水平对齐。
- 应用类卡片「进入」按钮 `disabled` 条件：`!onShelf || busy || !p.routePath`。
- 信任级别经 `trustLabel` 本地化释义：`untrusted（未信任）`，未知级别回退原值。

### 2.11.4 i18n 新增文案

| Key | 中文 | 英文 |
| --- | ---- | ---- |
| `plugins.page.tab.plugin` | 插件 | Plugins |
| `plugins.page.tab.app` | 应用 | Apps |
| `plugins.page.empty.plugin` | 暂无插件（业务内嵌 / surface 槽） | No plugins (embedded / surface slot) |
| `plugins.page.empty.app` | 暂无应用（独立路由注入） | No apps (standalone route injection) |
| `plugins.card.enter` | 进入 | Open |
| `plugins.card.trust.untrusted` | 未信任 | untrusted |
| `plugins.card.sidebar.on/off` | 有 / 无 | Yes / No |

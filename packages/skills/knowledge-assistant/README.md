## knowledge-assistant Skill（本仓库内置）

入口文档见 `SKILL.md`。

模板代码见 `templates/assistant/`，适用于在不同项目中快速落地“文档侧边助手”的：

- 会话隔离（按 `documentKey`）
- SSE 流式输出
- ephemeral（不落库）/持久化切换
- 迁入对话（import transcript）策略
- 与之配套的 UI 布局与样式类


# Markdown 外链默认新标签打开

> 延伸阅读：
> - [使用指南.md](./使用指南.md)（`@dnhyxc-ai/markdown-kit` 包整体使用）
> - [../monaco/Markdown预览目录哈希导航.md](../monaco/Markdown预览目录哈希导航.md)（页内 `#` 锚点滚动逻辑，外链拦截器与本改动互补）

## 1. 背景与目标

助手回复、知识库预览等场景渲染 Markdown 时，正文中的外部链接（`http://` / `https://`）默认在**当前标签页**打开。在 SPA（单页应用）和 Tauri WebView 环境中，同页导航会**顶掉整个应用**——用户点击链接后应用被替换为目标网页，需手动后退才能回来。

改动前，外链拦截靠宿主侧 `attachExternalLinkClickInterceptor` 在 `click` 事件中拦截 `href` 非页内锚点的 `<a>`，调系统浏览器打开。但这只在**已挂载拦截器的宿主**生效；若某处 Markdown 渲染未接拦截器，外链仍走浏览器默认行为。

本次改动在 **markdown-it 渲染层**直接给外链 `<a>` 补 `target="_blank"` + `rel="noopener noreferrer"`，使无论宿主是否挂了点击拦截器，外链都默认在新标签打开。页内 `#` 锚点除外，保持 SPA 内片段导航能力。

## 2. 改动范围

| 文件 | 改动 |
|------|------|
| `packages/markdown-kit/src/markdown/parser.ts` | 新增 `patchExternalLinksOpenBlank()` 私有方法，在 `configure()` 中调用 |
| `packages/markdown-kit/test/markdown/parser.test.ts` | 新增测试用例验证外链带 `target=_blank`、锚点不带 |
| `packages/markdown-kit/CHANGELOG.md` | 1.0.3 版本记录 |
| `packages/markdown-kit/package.json` | 版本号 `1.0.2 → 1.0.3` |

> `packages/markdown-kit/src/highlight/styles.ts` 与 `packages/markdown-kit/src/generated/highlight-js-theme-ids.ts` 亦在本轮改动中变更，但属**纯格式化**（单引号→双引号、多行→单行），无逻辑变化，此处不展开对比。

## 3. 实现思路

1. **在渲染层而非宿主层处理**：markdown-it 的 `renderer.rules.link_open` 是所有 `<a>` 标签渲染的必经之路。在此注入逻辑，比在每个宿主挂拦截器更可靠——一次修改覆盖所有使用该 parser 的场景。

2. **保留已有 `link_open` 逻辑**：用「保存 `prev` → 覆写 → 末尾调 `prev`」模式（monkey-patch），不破坏 markdown-it 内置或其它插件对 `link_open` 的处理。

3. **只补不覆盖**：用 `attrIndex('target') < 0` 判断已有 `target` 属性时不重复添加，尊重 Markdown 源码中手动写了 `target` 的场景。

4. **`#` 锚点除外**：`href.startsWith('#')` 的链接是页内片段导航，不补 `target="_blank"`，保持与 `useMarkdownHashLinkViewportScroll` / `attachMarkdownHashLinkNavigation` 的锚点滚动逻辑兼容。

5. **`rel="noopener noreferrer"`**：`_blank` 打开时浏览器默认 `opener` 非空，新页面可通过 `window.opener` 操作原页面（钓鱼攻击向量）。`noopener` 切断引用，`noreferrer` 兼容旧浏览器且不泄露 `Referer`。

## 4. 关键代码对比与注释

### 4.1 `configure()` 中调用 `patchExternalLinksOpenBlank`

**改动前** · `packages/markdown-kit/src/markdown/parser.ts`（基线，约 L355–L357）

```typescript
// GFM 任务列表补丁调用之后、LaTeX 分隔符之前
this.patchGfmTaskListBareMarkers();

// 此处无外链补丁调用
// Add support for \(...\) and \[...\] delimiters
this.addLatexDelimiters();
```

**改动后** · `packages/markdown-kit/src/markdown/parser.ts`（当前源码，约 L357–L361）

```typescript
// GFM 任务列表补丁调用之后
this.patchGfmTaskListBareMarkers();
// 新增：外链默认 _blank，在所有其它渲染补丁之前安装
this.patchExternalLinksOpenBlank();

// LaTeX 分隔符补丁不受影响
// Add support for \(...\) and \[...\) delimiters
this.addLatexDelimiters();
```

**变更摘要**：在 `patchGfmTaskListBareMarkers` 之后、`addLatexDelimiters` 之前插入一行调用 `patchExternalLinksOpenBlank()`。

### 4.2 `patchExternalLinksOpenBlank` 方法（纯新增）

> 本方法为**纯新增**，无「改动前」版本。按 `code-before-after.md` §4，纯新增符号只需贴改动后代码块。

**改动后** · `packages/markdown-kit/src/markdown/parser.ts`（当前源码，约 L428–L450）

```typescript
// JSDoc：说明方法用途——避免 SPA/WebView 同页导航顶掉应用
/**
 * 外链默认新标签打开（页内 `#` 锚点除外），避免 SPA / WebView 内同页导航顶掉应用。
 * 桌面壳仍建议宿主拦截点击走系统浏览器（见 Host `attachExternalLinkClickInterceptor`）。
 */
// 私有方法，只在 configure() 内调用一次
private patchExternalLinksOpenBlank(): void {
	// 取当前 markdown-it 实例
	const md = this.md;
	// 保存已有的 link_open 渲染规则；无则用默认 renderToken
	const prev =
		md.renderer.rules.link_open ||
		((tokens, idx, options, _env, self) =>
			self.renderToken(tokens, idx, options));
	// 覆写 link_open：在外链 <a> 上补 target / rel 属性
	md.renderer.rules.link_open = (tokens, idx, options, env, self) => {
		// 取 href 属性值并 trim，空串则视为无 href
		const href = tokens[idx].attrGet('href')?.trim() ?? '';
		// 非空且非页内锚点（# 开头）时补属性
		if (href && !href.startsWith('#')) {
			// 已有 target 时不覆盖（尊重 Markdown 源码手写 target）
			if (tokens[idx].attrIndex('target') < 0) {
				// 补 target="_blank"：新标签打开
				tokens[idx].attrPush(['target', '_blank']);
			}
			// 已有 rel 时不覆盖
			if (tokens[idx].attrIndex('rel') < 0) {
				// 补 rel="noopener noreferrer"：防钓鱼 + 不泄露 Referer
				tokens[idx].attrPush(['rel', 'noopener noreferrer']);
			}
		}
		// 调原始渲染逻辑输出 <a> 标签
		return prev(tokens, idx, options, env, self);
	};
}
```

### 4.3 测试用例（纯新增）

**改动后** · `packages/markdown-kit/test/markdown/parser.test.ts`（当前源码，约 L53–L64）

```typescript
// 测试标题：外链带 target=_blank，页内锚点不带
it('外链带 target=_blank，页内锚点不带', () => {
	// 用基础选项创建 parser 实例
	const parser = new MarkdownParser(baseParserOptions);
	// 渲染包含外链和锚点的 Markdown 源码
	const html = parser.render(
		'[外](http://127.0.0.1:9012/) 与 [锚](#sec)',
	);
	// 匹配外链 <a> 标签
	const external = html.match(/<a\b[^>]*href="http:\/\/127\.0\.0\.1:9012\/"[^>]*>/);
	// 断言外链带 target="_blank"
	expect(external?.[0]).toContain('target="_blank"');
	// 断言外链带 rel="noopener noreferrer"
	expect(external?.[0]).toContain('rel="noopener noreferrer');
	// 匹配页内锚点 <a> 标签
	const hash = html.match(/<a\b[^>]*href="#sec"[^>]*>/);
	// 锚点标签存在
	expect(hash?.[0]).toBeTruthy();
	// 锚点不带 target 属性
	expect(hash?.[0]).not.toContain('target=');
});
```

## 5. 兼容性与影响

- **向后兼容**：纯新增行为，不改变已有 `link_open` 逻辑。已有 `target` 属性的 `<a>` 不被覆盖。
- **与宿主拦截器互补**：宿主侧 `attachExternalLinkClickInterceptor` 仍可拦截 `click` 走系统浏览器（桌面 Tauri）；渲染层 `target="_blank"` 作为兜底，确保未接拦截器的场景也安全。
- **页内锚点不受影响**：`#` 开头的 href 不补 `target`，`useMarkdownHashLinkViewportScroll` / `attachMarkdownHashLinkNavigation` 的锚点滚动逻辑正常工作。

## 6. 相关源码路径

| 说明 | 路径 |
| ---- | ---- |
| parser 主文件 | `packages/markdown-kit/src/markdown/parser.ts` |
| 测试 | `packages/markdown-kit/test/markdown/parser.test.ts` |
| 变更日志 | `packages/markdown-kit/CHANGELOG.md` |
| 包配置 | `packages/markdown-kit/package.json` |

---

若与仓库最新源码不一致，以源码为准。
